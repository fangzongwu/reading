import React, {
	Component
} from "react";
import {
	View,
	Text,
} from "react-native";
import {
	StackNavigator,
} from "react-navigation";

export default class Navigators extends Component {
	constructor(props) {
		super(props);
	};
	render() {
		return (
			<View>
				<Text>123</Text>
			</View>
		);
	}
}